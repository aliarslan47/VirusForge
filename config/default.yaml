# VirusForge varsayılan config. Kullanıcı config'i bunu derin-merge ile ezer.
general:
  threads: 8
  memory_gb: 32
  mode: auto            # auto | short | long | hybrid | assembly  (override)
  keep_native: true

detect:
  short_max_len: 500    # ortalama read uzunluğu bu altındaysa short adayı
  long_min_len: 1000    # bu üstündeyse long adayı

tools:
  fastp:
    extra_args: ""
  filtlong:
    min_length: 1000
    keep_percent: 90
  spades:
    careful: true
  flye:
    # kimya header'dan; override: r9 | r10 | none
    chemistry: auto
  medaka:
    model: auto         # kimyaya göre; override edilebilir
  unicycler:
    mode: normal
  genomad:
    db: databases/genomad
  mash:
    inphared_sketch: databases/inphared/inphared.msh
  pharokka:
    db: databases/pharokka
  checkv:
    db: databases/checkv
  phabox:
    db: databases/phabox
    conda_env: vf_phabox          # phabox izole env (pandas<2 uyumlu)
    conda_bin: /home/ali/miniconda3/bin/conda
  # --- M2-A faj zenginleştirme (izole env'ler — çalışan virusforge env'i korunur) ---
  host:
    method: rafah                 # rafah (varsayılan) | iphop
    rafah_db: databases/rafah     # RaFAH modeli kendi içinde; gerekirse yol
    iphop_db: databases/iphop     # opsiyonel — dev'de indirilmez (>200GB)
    conda_env: vf_rafah           # boş bırak = araç ana env'de
    conda_bin: /home/ali/miniconda3/bin/conda
  amrfinder:
    db: ""                        # boş = AMRFinderPlus varsayılan kurulum DB
    use_abricate: false           # opsiyonel VFDB/CARD hook (varsayılan kapalı)
    conda_env: vf_amr
    conda_bin: /home/ali/miniconda3/bin/conda
  phageterm:
    extra_args: ""
    conda_env: vf_phageterm
    conda_bin: /home/ali/miniconda3/bin/conda
  phold:
    db: ""                        # boş = phold varsayılan DB
    conda_env: vf_phold
    conda_bin: /home/ali/miniconda3/bin/conda

optional:                # default kapalı (tasarım kararı)
  virsorter2: false
  vibrant: false
  sourmash: false
  phold: false
  kraken2: false
